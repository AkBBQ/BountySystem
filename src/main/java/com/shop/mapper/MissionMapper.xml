<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.MissionMapper">
  <resultMap id="BaseResultMap" type="com.shop.model.Mission">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="type" jdbcType="INTEGER" property="type" />
    <result column="content" jdbcType="VARCHAR" property="content" />
    <result column="start_time" jdbcType="DATE" property="startTime" />
    <result column="end_time" jdbcType="DATE" property="endTime" />
    <result column="place" jdbcType="VARCHAR" property="place" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="lock" jdbcType="INTEGER" property="lock" />
    <result column="pid" jdbcType="INTEGER" property="pid" />
    <result column="title" jdbcType="VARCHAR" property="title" />
  </resultMap>
  
  <select id="queryAllMissions" parameterType="Mission" resultMap="BaseResultMap">
    select * from mission
    <where>
      <if test="null != id and id != '' ">
        and id=#{id}
      </if>

      <if test="type != null and type != '' ">
        and type=#{type}
      </if>

      <if test="status != null and status != '' ">
        and status=#{status}
      </if>

      <if test="lock != null and lock != '' ">
        and lock=#{type}
      </if>

      <if test="startTime != null and startTime != '' ">
        and start_time &gt;= #{startTime}
      </if>

      <if test="endTime != null and endTime != '' ">
        and end_time  &lt;= #{endTIme}
      </if>

    </where>
    LIMIT #{offset},#{pagesize}
  </select>

  <select id="count" parameterType="Mission" resultType="integer">
    select count(1) from mission
    <where>
      <if test="null != id and id != '' ">
        and id=#{id}
      </if>

      <if test="type != null and type != '' ">
        and type=#{type}
      </if>

      <if test="status != null and status != '' ">
        and status=#{status}
      </if>

      <if test="lock != null and lock != '' ">
        and lock=#{type}
      </if>

      <if test="startTime != null and startTime != '' ">
        and start_time &gt;= #{startTime}
      </if>

      <if test="endTime != null and endTime != '' ">
        and end_time  &lt;= #{endTIme}
      </if>


    </where>
  </select>

    <insert id="addMission" parameterType="Mission">
        insert into mission(type,content,creat_time,end_time,place,status,lock,pid,title)
        values(#{type},#{content},#{creatTime},#{endTime},#{place},#{status},#{lock},#{pid},#{title})
    </insert>
</mapper>